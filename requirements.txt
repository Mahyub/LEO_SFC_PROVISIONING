# Core solver
pulp>=2.7

# Numerical
numpy>=1.24

# Figures (optional -- only needed for --mode figures)
matplotlib>=3.7

# MATLAB bridge (optional -- only needed for --mode matlab_*)
scipy>=1.10

# Testing
pytest>=7.4
